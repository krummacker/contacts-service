image: golang:latest

pipelines:
  default:
    - step:
        name: Check
        script:
          - uname -a
          - go version
          - go test -v $(go list ./...)
          - go vet $(go list ./...)


